plugins {
}

repositories {
}

bootJar.enabled=false

dependencies {

    api project(":domain")

    implementation 'org.springframework.boot:spring-boot-starter-data-elasticsearch'
}

test {
    useJUnitPlatform()
}